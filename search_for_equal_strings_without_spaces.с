#include <stdio.h>

void copy(char s1[100], char s2[100]); // s1 копируется в s2

//result: 0 - s1 > s2; 1 - s1 < s2; 2 - s1 = s2
int comparison(char s1[100], char s2[100]); 
void delete_space(char s[100]);
int getlin(char s[100]);
void putline(char s[100]);
void arr_in_string(char arr[2000][100], char s[100], int k);
void string_in_arr(char s[100], char arr[2000][100], int k);
void arr_sort(char arr[2000][100], int n);

int main() {
    char arr[2000][100];
    int n = 0;
    while (getlin(arr[n]) != 0)
    {
        //delete_space(arr[n]);
        putline(arr[n]);
        n++;
    }
    for (int i = 0; i < n; i++)
        putline(arr[n]);
}

int getlin(char s[100]) {
    int c = ' ';
    int len = 0;
    
    while ((c = getchar()) != EOF && c != '\n' && c != '|') {
        s[len] = c;
        len++;
    }
    if (c == '\n') {
        s[len] = '\n';
        len++;
        s[len] = '\0';
    }
    return len;
}

void copy(char s1[100], char s2[100]) {
    int i = 0;
    
    while (s1[i] != '\0') {
        s2[i] = s1[i];
        i++;
    }
    
    s2[i] = '\0';
}

int comparison(char s1[100], char s2[100]) {
    int i = 0;
    
    while (s1[i] != '\0') {
        if (s1[i] != s2[i]) {
            break;
        }
        i++;
    }
    if (s1[i] == s2[i]) return 2;
    if (s1[i] == '\0') return 1;
    if (s2[i] == '\0') return 0;
    if (s1[i] > s2[i]) return 0;
    if (s1[i] < s2[i]) return 1;
    return 0;
}

void delete_space(char s[100]) {
    char s1[100];
    int i = 0;
    int j = 0;
    while (s[i] != '\0') {
        if (s[i] != ' ') {
            s1[j] = s[i];
            j++;
        }
        i++;
    }
    s1[j] = '\0';
    copy(s1, s);
}

void putline(char s[100]) {
    int i = 0;
    
    while (s[i] != '\0') {
        putchar(s[i]);
        i++;
    }
}

void arr_in_string(char arr[2000][100], char s[100], int k) {
    int i = 0;
    while (arr[k][i] != '\0') {
        s[i] = arr[k][i];
        i++;
    }
    s[i] = '\0';
}

void string_in_arr(char s[100], char arr[2000][100], int k) {
    int i = 0;
    while (s[i] != '\0') {
        arr[k][i] = s[i];
        i++;
    }
    arr[k][i] = '\0';
}

void arr_sort(char arr[2000][100], int n){
    for (int j = 1; j < n; j++)
        for (int i = 0; i < n - 1; i++) {
            if (comparison(arr[i], arr[i+1]) == 0)
            {
                char s[100];
                copy (arr[i], s);
                copy (arr[i+1], arr[i]);
                copy (s, arr[i+1]);
            }
        }
        
}
